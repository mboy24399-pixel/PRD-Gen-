# PRD Forge is intentionally local-first and deterministic.
# No AI API keys, LLM credentials, server database, or backend environment variables are required.
# All project data is stored in IndexedDB in the user's browser.
